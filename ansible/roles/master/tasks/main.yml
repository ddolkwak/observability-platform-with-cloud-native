---
# tasks file for master
# 1. Init Kubeadm
- name: Initialize Kubernetes cluster
  command: kubeadm init --apiserver-advertise-address=192.168.56.130 --pod-network-cidr=10.244.0.0/16
  args:
    creates: /etc/kubernetes/admin.conf

# 2. Enable Admin User Using Kubectl
- name: Create .kube directory for admin user
  file:
    path: /home/admin/.kube
    state: directory
    owner: admin
    group: admin
    mode: '0755'

- name: Copy admin.conf to admin user's kubeconfig
  copy:
    src: /etc/kubernetes/admin.conf
    dest: /home/admin/.kube/config
    remote_src: yes
    owner: admin
    group: admin
    mode: '0600'

# 3. Symlink Kubeconfig for root
- name: Ensure .kube directory exists for root user
  file:
    path: /root/.kube
    state: directory
    mode: '0700'

- name: Link admin kubeconfig to root kubeconfig
  file:
    src: /home/admin/.kube/config
    dest: /root/.kube/config
    state: link

- name: Add Alias K(Kubectl) on Admin
  lineinfile:
    path: /home/admin/.bashrc
    line: "alias k='kubectl'"
    regexp: "^alias k="
    owner: admin
    group: admin
    mode: '0644'

# 4. Install Flannel CNI Network Plugin
#- name: Install Flannel Pod Network
#  command: kubectl apply -f https://github.com/flannel-io/flannel/releases/latest/download/kube-flannel.yml
#  become_user: admin

# 5. Extract Join Command (for Worker Join)
- name: Generate and Delegate Join Command
  block:
    - name: Generate kubeadm join command
      command: kubeadm token create --print-join-command
      register: join_command_raw

    - name: Set Join Command for Workers
      set_fact:
        extracted_join_cmd: "{{ join_command_raw.stdout }}"
      delegate_to: "{{ item }}"
      delegate_facts: true
      loop: "{{ groups['workers'] }}"
